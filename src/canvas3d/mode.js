module.exports = {
	init(particles) {
		this.p = particles;
	},
	sync(delta) {
		//this.p.deg += delta;
		//this.particles.uniforms.uPx.value = Math.cos((Math.PI / 180) * this.particles.deg) * this.particles.radius;
		//this.particles.uniforms.uPy.value = Math.sin((Math.PI / 180) * this.particles.deg) * this.particles.radius;
	},
};
